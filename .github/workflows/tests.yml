name: Tests

on:
    pull_request:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Run Jest
      run: npm
    - name: Run Tests
      run: npm run test
      